{
  "name": "gas-pages-template",
  "version": "1.0.0",
  "description": "gas pages template application.",
  "homepage": "https://github.com/tutttuwi/gas-pages-template#readme",
  "bugs": {
    "url": "https://github.com/tutttuwi/gas-pages-template/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tutttuwi/gas-pages-template.git"
  },
  "license": "MIT",
  "author": "tutttuwi",
  "type": "commonjs",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "push": "pnpm build && clasp push --force",
    "deploy": "node -e \"const id=process.env.DEPLOYMENT_ID; if(!id||id==='your-deployment-id'){console.error('Set DEPLOYMENT_ID to a real clasp deployment ID. List IDs with: pnpm exec clasp deployments'); process.exit(1)}\" && pnpm push && clasp deploy --deploymentId=\"$DEPLOYMENT_ID\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@google/clasp": "^3.2.0"
  }
}
